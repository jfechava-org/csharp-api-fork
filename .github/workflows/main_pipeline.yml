## Comment for testing workflow
name: main_pipeline
on:
  push:
    branches:
      - main
      - 'feature/**'

jobs:
  build:
    #runs-on: 'windows-2019'
    runs-on: 'ubuntu-20.04'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET Core
        uses: actions/setup-dotnet@v3
        with: 
          dotnet-version: '6.0'

      - name: Restore Dependencies
        run: dotnet restore

      - name: Build Microservice
        run: |
          # build your application here
          echo "Building application..."
          dotnet build --configuration Release
  
      - name: Test Microservice
        run: dotnet test

      - name: Publish Artifact
        if: success()
        run: dotnet publish -c Release -o publish

      - name: Build Docker Image
        uses: docker/build-push-action@v5.0.0
        with:
          context: ./myMicroservice
          file: ./myMicroservice/Dockerfile
          platforms: linux/amd64  # Adjust architecture as needed
          push: true

      - name: Set up Google Cloud CLI
        uses: GoogleCloudPlatform/github-actions/setup-gcloud@main
        with:
          service_account_key: 'bdab3f80aa4dc5d33ec49a9b712a1915fea5f775'   #${{ secrets.GCP_SA_KEY }}
          project_id: 'github-workflows-400912'   #${{ secrets.GCP_PROJECT_ID }}
    
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install dependencies
        run: |
          # install any dependencies needed for testing
          echo "Installing dependencies..."
          sleep 15

      - name: Run tests
        run: |
          # run your tests here
          echo "Running tests..."
          sleep 15

  deploy:
    runs-on: ubuntu-latest
    needs: [build, test]
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy application
        run: |
          # deploy your application here
          echo "Deploying application..."
          sleep 15
